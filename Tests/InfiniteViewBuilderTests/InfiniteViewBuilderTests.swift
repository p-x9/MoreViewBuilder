import XCTest
@testable import InfiniteViewBuilder

final class InfiniteViewBuilderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(InfiniteViewBuilder().text, "Hello, World!")
    }
}
